function keyPressed(){
    setTimeout (function() {
    if (state == 0)
        if (key == '1'){
            
            showAlternateImage = showAlternateImage ? false : true; 
}
}, 2000);

    if (state == 1)
    if (key == '2'){
        
        showAlternateImage = showAlternateImage ? false : true; 
}
    if (state ==2)
    if (key == '3'){
        
        showAlternateImage = showAlternateImage ? false : true; 
}
if (state == 3)
    if (key == '4'){
        
        showAlternateImage = showAlternateImage ? false : true; 
}
if (state == 4)
    if (key == '5'){
        
        showAlternateImage = showAlternateImage ? false : true; 
}
}